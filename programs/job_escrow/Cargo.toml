[package]
name = "job-escrow"
version = "0.2.0"
description = "MoltCities Job Board Escrow Program - Pinocchio Edition"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "job_escrow"

[features]
default = []
bpf-entrypoint = []

[dependencies]
pinocchio = "0.9"
pinocchio-system = "0.4"
